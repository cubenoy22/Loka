[rig]
descriptor_version = 1
rig_id = toolbox-maciix
machine = maciix
build_architecture = m68k
build_profile = retro68-68k-release
supported_modes = flow
disposable_for_input = true
capture_adapter = mame-screen-snapshot.v2
recording_adapter = manual
artifact_contract_version = 1
# The identity_sha256 printed by the #516 window-mask re-bake, reviewed here
# rather than written by the bake. The bake never edits this tracked authority
# and so cannot authorize its own output; a bundle is verdict-eligible only
# when its recorded identity matches this value.
#
# Approved 2026-08-29 for capture_adapter mame-screen-snapshot.v2, which
# compares the application window's own pixels through the golden's alpha mask
# instead of the whole 640x480 screen (GH #516). Every other identity field is
# unchanged from the v1 baseline: the rig did not move, only what is compared.
#
# Baked 2026-08-29 on Omen at main = 20be6ca3 plus the #516 change, all 16
# registered cells. Toolchain GCC 16.1.0 / Universal Interfaces 0x0340, MAME
# maciix, RAM 8M, boot_hd_sha256 415438ed... (the pinned read-only template).
#
# Re-baked 2026-09-14 on Omen at main = b049345e, all 24 registered cells in
# one run-all-cells.sh --update-golden pass (#737), reviewed and approved by
# the project owner. The identity covers the rig environment only and none of
# it moved, so the value below is unchanged; the captures reflect #752
# (RectSurface clipped to its bounds) and #753 (Toolbox baseline nodes placed
# from the box top).
#
# Re-baked 2026-09-20 on Omen at main = 9b2bba43, all 25 registered cells in
# one run-all-cells.sh --update-golden pass after #844 added
# smirkbench/attributed-editor-line (#834 acceptance), reviewed and approved by
# the project owner. The 24 earlier captures are byte-identical to the
# 2026-09-19 a3ca72d5 bundle; the rig environment did not move, so the value
# below is unchanged.
reference_identity_sha256 = 8eedf39aed8c900823809d0ef3b7312bedba2d6a6403c676eaf4e0ca62119326
