[rig]
descriptor_version = 1
rig_id = toolbox-maciix
machine = maciix
build_architecture = m68k
build_profile = retro68-68k-release
supported_modes = flow
disposable_for_input = true
capture_adapter = mame-screen-snapshot.v2
recording_adapter = manual
artifact_contract_version = 1
# The identity_sha256 printed by the 0.0.3a re-bake, reviewed here rather than
# written by the bake. The bake never edits this tracked authority and so
# cannot authorize its own output; a bundle is verdict-eligible only when its
# recorded identity matches this value.
#
# Baked 2026-08-22 on Omen at main = 9d0eceba, all 17 registered cells.
# Toolchain GCC 16.1.0 / Universal Interfaces 0x0340, MAME maciix, RAM 8M,
# boot_hd_sha256 415438ed... (the pinned read-only template).
reference_identity_sha256 = 080c51416bcac65bb9e621709e8666dbf06fef4acf9930b94ba4caf4e929b15d
