[rig]
descriptor_version = 1
rig_id = toolbox-maciix
machine = maciix
build_architecture = m68k
build_profile = retro68-68k-release
supported_modes = flow
disposable_for_input = true
capture_adapter = mame-screen-snapshot.v1
recording_adapter = manual
artifact_contract_version = 1
# The 0.0.3a re-bake must replace this marker with the separately reviewed
# identity_sha256 printed by a complete bundle bake. The bake itself never
# edits this tracked authority and therefore cannot authorize its own output.
reference_identity_sha256 = unapproved
