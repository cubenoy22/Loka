#ifndef LOKA_TESTS_TOOLBOX_STANDALONE_FLOW_DRIVER_HPP
#define LOKA_TESTS_TOOLBOX_STANDALONE_FLOW_DRIVER_HPP

namespace loka
{
  namespace toolbox_tests
  {
    /** Runs the compiled presentation Flow without external configuration. */
    int RunStandaloneFlowApplication();
  }
} // namespace loka

#endif // LOKA_TESTS_TOOLBOX_STANDALONE_FLOW_DRIVER_HPP
