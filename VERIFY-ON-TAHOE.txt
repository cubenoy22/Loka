GH #251 macOS verification (Tahoe rig)

Green tree:

  cmake --preset macos-debug
  cmake --build --preset macos-tests
  build/macos/Debug/apple/macos/LokaTestsMacOS testMacNodeHandlerEnsureContract
  ctest --preset macos-tests -R '^LokaTestsMacOS$' --output-on-failure

Red mutation:

  In common/app/scene/projection/RetainedNodeHandler.hpp, temporarily replace

    return static_cast<CtxT *>(typed->getContext());

  with

    return ctx;

  Rebuild with `cmake --build --preset macos-tests`, then run
  `build/macos/Debug/apple/macos/LokaTestsMacOS testMacNodeHandlerEnsureContract`.
  The test must fail at the re-entrant prepareProjectedLayout check because
  the stale local is non-null. Restore the node re-read, rebuild, and confirm
  the focused test and filtered ctest command above pass.
